{
	"name": "mailnick",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "bunx --bun vite dev",
		"build": "bunx --bun vite build",
		"preview": "vite preview",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
		"build:binary": "bun run build && cd build && bun build --compile --minify ./index.js --outfile ../dist/mailnick"
	},
	"devDependencies": {
		"@sveltejs/adapter-auto": "^3.0.0",
		"@sveltejs/kit": "^2.0.0",
		"@sveltejs/vite-plugin-svelte": "^5.0.0",
		"svelte": "^5.0.0",
		"svelte-check": "^4.0.0",
		"typescript": "^5.0.0",
		"vite": "^6.0.0",
		"svelte-adapter-bun": "^0.5.0",
		"drizzle-kit": "^0.27.0"
	},
	"dependencies": {
		"@google/genai": "^1.41.0",
		"bootstrap": "^4.6.2",
		"drizzle-orm": "^0.36.0",
		"googleapis": "^144.0.0"
	}
}
